# TODO branch update
name: 'Verify and Release'

on:
  push:
    branches:
      - next

jobs:
  publish:
    uses: trendyol/baklava-icons/.github/workflows/publish-storybook.yml@next
    secrets: inherit
  release:
    uses: trendyol/baklava-icons/.github/workflows/release.yml@next
    needs: publish
    secrets: inherit
  chromatic:
    uses: trendyol/baklava-icons/.github/workflows/publish-chromatic.yml@next
