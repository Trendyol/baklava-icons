import { Meta } from "@storybook/blocks";
import { IconList } from "./IconList";
import IconListStories from "./IconList.stories";

<Meta of={IconListStories} title="Icons"  />

# Baklava Icons

- All icons are provided as SVGs
- All icons are single color (Some have multiple layers with different opacity of same color)
- All icons has square canvas (By default 24x24)
- Icon names are in `snake_case`
- Icons are for decorative purposes. Don't use them as a data in your documents (like an instructive image)

You can check [bl-icon component](https://baklava.design/?path=/docs/components-icon--documentation) for usage information and interactive demos.

## List of icons

<IconList />
